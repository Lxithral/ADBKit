# ADBKit

一个简洁的 Android ADB 调试快捷工具，支持 Root 快速开关各项调试功能。

## 功能

- **开发者选项** — 一键开关
- **USB 调试** — 一键开关
- **USB 安装** — 一键开关（支持 MIUI）
- **USB 调试安全设置** — 一键开关
- **无线调试** — 一键开关，显示连接地址和端口（兼容 Android 17/HyperOS 不写入 `service.adb.tls.port` 的机型）
- **快捷磁贴** — 5 个 Quick Settings Tile，下拉控制中心即可操作
- **长按磁贴打开应用** — 长按任意磁贴即可跳转打开 App
- **内存管理** — 遵循公平运行内存机制，磁贴操作完自动退出，不常驻后台

## 界面

- MIUI 风格 UI（[miuix](https://github.com/compose-miuix-ui/miuix) 组件库）：大标题折叠顶栏、圆角卡片、MIUI 风格设置项
- 底部栏三形态：标准 / 悬浮胶囊 / 液态玻璃（边缘折射透镜），设置内即时切换
- 主题：跟随系统 / 浅色 / 深色，状态栏图标随深浅色自动适配

## 特性

- 磁贴操作后自动退出：触发 10 秒自杀计时；若被 SystemUI 重新拉起会重新武装计时，直到系统解除磁贴绑定后进程真正退出
- 使用保护：应用在前台或面板展开浏览磁贴时不会自杀，连续点击自动重置计时
- 液态玻璃/毛玻璃基于 RuntimeShader，设备不支持时自动降级为不透明样式
- 合并 Shell 命令，单次轮询 10 秒

## 要求

- Android 13+ (API 33)
- Root 权限

## 安装

从 [Releases](../../releases) 下载 APK 安装。

## 更新日志

### 1.0.4

- 新增：底部栏三形态——标准 / 悬浮 / 液态玻璃，外观设置内即时切换
- 新增：外观设置（主题：跟随系统 / 浅色 / 深色；底栏形态选择）
- 修复：无线调试不显示端口（兼容 Android 17 / HyperOS 不写入 `service.adb.tls.port` 的机型）
- 工程：MIUIX 升级至 0.9.4

### 1.0.3

- 修复：仅通过状态栏磁贴操作后，进程仍常驻后台的问题（自杀后进程被 SystemUI 重新拉起，现会重新武装自杀计时，直至系统解绑后进程真正退出）
- 新增：长按状态栏磁贴打开 App（`QS_TILE_PREFERENCES`）
- 工程：支持中文路径下构建（`android.overridePathCheck=true`），修复 `drawable/avatar` 重复资源导致的构建失败
